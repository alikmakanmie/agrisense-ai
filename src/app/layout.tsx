import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AgriSense AI — Agri-Business Strategy Agent",
  description:
    "An intelligent agri-business strategy agent powered by AI. Analyze land, soil, and weather conditions to get actionable farming recommendations from a veteran agronomy consultant.",
  keywords: [
    "agriculture",
    "AI",
    "agribusiness",
    "strategy",
    "agronomy",
    "farming",
    "consultant",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
